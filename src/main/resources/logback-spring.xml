<configuration>

    <!-- Define log file path -->
    <property name="LOG_FILE" value="C:/Logs/Nuziveedu-Seeds-Operator-Interface.log" />

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling File Appender -->
    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>

        <!-- Rotate based on size and date -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- archived log file pattern -->
            <fileNamePattern>C:/Logs/Nuziveedu-Seeds-Operator-Interface.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>

            <!-- max file size before rolling -->
            <maxFileSize>10MB</maxFileSize>

            <!-- keep 30 days of history -->
            <maxHistory>30</maxHistory>

            <!-- total size cap for all archived files -->
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root Logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="ROLLING_FILE" />
    </root>

</configuration>
